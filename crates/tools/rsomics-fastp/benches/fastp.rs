//! Criterion benches for `rsomics-fastp` against upstream `fastp`.
//!
//! Skeleton — populated once the preprocessing pipeline lands. Runs against
//! tier-2 fixtures fetched per `tests/fixtures-manifest.toml`.

fn main() {
    println!("rsomics-fastp benches: not yet implemented");
}
